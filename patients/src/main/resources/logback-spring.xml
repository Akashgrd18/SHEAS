<configuration>
    <appender name="patientFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file> Patients/logs/patientFile.logs</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- daily rollover and up to 10MB per file -->
            <fileNamePattern>logs/patientFile.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
<!--        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">-->
<!--            <level>WARN</level>-->
<!--        </filter>-->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="patientConsole" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level - %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <logger name="com.project.patients" level="INFO">
        <appender-ref ref="patientFile"/>
    </logger>

    <root level="INFO" >
        <appender-ref ref="patientConsole"/>
    </root>


</configuration>